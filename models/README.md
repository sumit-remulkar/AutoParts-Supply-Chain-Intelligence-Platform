# Models

Trained model artifacts (risk scorecard weights, forecasting model
checkpoints) and their backtested evaluation reports land here.
Large binary artifacts should use Git LFS or be excluded and documented
with a reproducible training script instead.

🚧 Populated once the forecasting and risk-scoring models are trained.
